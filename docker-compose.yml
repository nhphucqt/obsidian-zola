version: '2'

services:
  obsidian_server:
    build:
      context: .
    container_name: obsidian
    restart: always
    environment:
      - TZ=Asia/Taipei
      - SITE_URL=""
    volumes:
      - /home/nhphucqt/Documents/Obsidian Vault/Main:/obsidian
    ports:
      - "1111:1111"
    command: sh local-run.sh
